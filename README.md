# TEMPLATE DATA ANALYTICS PROJECT

## Configuration
### File settings
There are two ways to set up the file paths.
- `config.py`
- `.env`
### Scheme settings
In `schemes` directory, we can define input/output schemes.